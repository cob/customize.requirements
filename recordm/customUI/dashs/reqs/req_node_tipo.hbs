{{#if (eq @root.selectedNode.value.0.level '1') }}
	{{#if (eq @root.selectedNode.value.0.name 'Lojas') }}
		Distrito
	{{else if (eq @root.selectedNode.value.0.name 'Entrepostos')}}
		Entreposto
	{{else if (eq @root.selectedNode.value.0.name 'Fornecedores')}}
		Fornecedor
	{{else if (eq @root.selectedNode.value.0.name 'Templates')}}
		Template
	{{/if}}
{{else if (eq @root.selectedNode.value.0.level '2') }}
	{{#if (eq @root.selectedNode.value.0.level_1 'Lojas') }}
		Loja
	{{!-- Não usei um OR de proposito para ser mais facil ler --}}
	{{else if (eq @root.selectedNode.value.0.level_1 'Fornecedores') }}
		Questionário
	{{else if (eq @root.selectedNode.value.0.level_1 'Entrepostos') }}
		Questionário
	{{/if}}
{{else if (eq @root.selectedNode.value.0.level '3') }}
	{{#if (eq @root.selectedNode.value.0.level_1 'Lojas') }}
		Questionário
	{{/if}}
{{/if}}